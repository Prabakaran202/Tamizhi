import "core/http_runtime.c";

fun listen(Num port) {
    __native_socket_listen(port); 
}

fun accept() {
    return __native_socket_accept(); 
}

fun send(Num client, Num status, Str msg) {
    __native_socket_send_response(client, status, msg); 
}
